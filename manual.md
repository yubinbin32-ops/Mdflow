mdflow mac app 产品文档
包含内容：app，mcp，skill，后两者包装成codex插件，在app的设置中可以一键安装
设计风格：精密数学工程艺术，ios高级风格，以白色调为主，统一颜色和动态效果
解决痛点：在codex从零开发过程中，需要和ai探讨好设计方案，形成多个开发文档来保证项目不会失真，同时能够及时同步进度，一个项目文档个同时存在以下内容：ui设计文档，产品文档，产品架构文档，api与数据库设计文档，checklist，修改历史等等，而且随着开发，文档会越来越冗余，导致严重影响ai上下文，反复的重构会导致内容失真，从而让ai在开发过程中导致方向偏移或者不知所错，而且文档大部分内容都是现在式和将来式，包含todo，ai对于架构需要通读上下文才能了解，严重影响上下文。
价值理念：未来的项目不会再使用md文档，由mdflow完全替代，agent能过精准的获取它任务所需要的相关信息来获取上下文，而不是通读。
参考：https://github.com/tt-a1i/archify%E3%80%82，/Users/a1-6/Documents/GitHub/topic/docs/TOPIC\_MANUAL.md
具体实现：项目是由一个个块和链组成的，块是抽象的，比如功能块，ui块等等块，是抽象的不是具体的，比如：接受图片并且分析图片结构。这个块和一个ui块绑定，比如：接受图片后出现跳动的点表示收到了，采用ios克制语言设计。链表示关系，比如a块和b块链接，表示执行顺序，前端的块可以和后端的块链接，中间的链接就是api，点击可以查看api数据规范。块通过链接可以形成一个chain，这个chain代表更为抽象的功能，比如根据图片设计提示词，这个功能链就包含多个块，a接收图片并分析内容，驱动b的ui块，通过接口c传递到后端的d功能块根据分析内容设计提示词，再传递给eui块在前端呈现。可以自由组合功能链并设计checkpoint来检测是否正常，包含修改历史，错误原因等等内容，app包含多个视图，因为块有很多类型，比如数据库块，ui块等等，用户/ai可以选择性呈现，视图是一个多选题，选中什么就呈现对应的块并且正确链接，不选中就隐藏，但是也要保重正确的链接，比如有一些块是a->api块b-〉c，如果隐藏了api视图，应该展示a->c。ai通过mcp来操作，项目的核心是ai不在需要反复删除和修改文档来减少上下文，可以保留完整的架构，它只需要读取自己需要的内容即可。用户在app中只能看，不能修改，它只能修改，配置插件，你可以自由设计block和link的形状和颜色以及动态效果来标明不同的状态，比如在chain中某一个block有问题，这个block可以变成错误的红色。mdflow不接收md文档，它只通过mcp开放接口，ai可以调用方法，ai可以像打开文件夹一样选择自己需要的内容和视图，而不是将mdflow的内容全部呈现，而是层层打开，ai读取时，需要将mdflow的内容抽出来形成md格式方便ai阅读，写入的时候也不能写入大量结构化数据防止大量输入输出，同样会影响上下文。
app的呈现主体上是一个canvas，参考上面的库设计的网图，需要有丰富的动态效果，内容之间自动排版，不能互相遮挡，连接的链不要使用贝塞尔曲线，而是用90度折线与直线连结。在开发文档中有很多前置内容，比如技术规范和要求，需要作为独特的块，这些块不和任何内容连接，比如ui的前置块，在返回和ui相关上下文的时候需要带有这个块的内容。在ui的呈现中采用优先级展示法，由小块聚合而成的chain作为一个块展示，点击后这个块展开看到内部的块的关系，动画衔接流畅，ai的调用也类似。
修改历史应该与块/chain绑定，ai在查看的时候不是返回所有的修改历史，而是只返回和感兴趣部分的历史。
ui中包含一些独立块，比如todo plan等，可以带有优先级呈现不同颜色，但是这些plan在执行之前要先在mdflow中新建成对应的架构才能开始实现，可以组合成chain，对于不同状态的块/chain，颜色不同，比如这些还没有开始实现的，可以设计成灰色，测试通过的显示成绿色，错误的红色，反复错误的，ai怀疑的，即将测试的等等等等。
上面的文档设计的不完整，需要根据实际内容进行调整并设计，在这个项目开发的过程中要实现自举，在可以的时候清除mdflow的文档并且写入mdflow，自己开发自己。在你开发的过程中你能够了解目前的架构能否真正替代文档并且做的更好，及时调整从而达到完美。
app的logo放在文件夹中。


